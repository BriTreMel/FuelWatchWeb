<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE struts-config PUBLIC
          "-//Apache Software Foundation//DTD Struts Configuration 1.3//EN"
          "http://jakarta.apache.org/struts/dtds/struts-config_1_3.dtd">


<struts-config>
    <form-beans>
        <form-bean name="GaugeUsage" type="com.myapp.struts.GaugeUsage"/>
        <form-bean name="OilUsage" type="com.myapp.struts.OilUsage"/>
        <form-bean name="OilUsage" type="com.myapp.struts.OilUsage"/>
        <form-bean name="ProductForm" type="com.myapp.struts.ProductForm"/>
        <form-bean name="LoginForm" type="com.myapp.struts.LoginForm"/>
       
        <form-bean name="RegisterForm" type="com.myapp.struts.RegisterForm"/>
    
    </form-beans>
    
    <global-exceptions>
    
    </global-exceptions>

    <global-forwards>
        <forward name="welcome"  path="/Welcome.do"/>
    </global-forwards>

    <action-mappings>
        <action name="RegisterForm" path="/register" scope="request" type="com.myapp.struts.RegisterAction" validate="false">
            <forward name="success" path="/success.jsp"/>
        </action>
        
        <action name="LoginForm" path="/login" scope="request" type="com.myapp.struts.LoginAction" validate="false">
            <forward name="admin" path="/registerNewCust.jsp"/>
            <forward name="success" path="/loginSuccess.jsp"/>
            <forward name="failure" path="/error.jsp"/>
            
        </action>
        
        <action name="ProductForm" path="/registerProd" scope="request" type="com.myapp.struts.ProductAction" validate="false">
        <forward name="success" path="/productSucces.jsp"/>
        <forward name="failure" path="/productError.jsp"/>
        </action>
        <action name="OilUsage" path="/oilinfo" scope="request" type="com.myapp.struts.OilAction" validate="false">
        <forward name="success" path="/oilSuccess.jsp"/>
        <forward name="failure" path="/oilFailure.jsp"/>   
        </action>
        <action name="GaugeUsage" path="/gaugeInfo" scope="request" type="com.myapp.struts.GaugeAction" validate="false">
        <forward name="success" path="/gaugeSuccess.jsp"/>
        <forward name="failure" path="/gaugeFailure.jsp"/>
        </action>
    </action-mappings>
    
    <controller processorClass="org.apache.struts.tiles.TilesRequestProcessor"/>

    <message-resources parameter="com/myapp/struts/ApplicationResource"/>    
    
    <!-- ========================= Tiles plugin ===============================-->
    <!--
    This plugin initialize Tiles definition factory. This later can takes some
    parameters explained here after. The plugin first read parameters from
    web.xml, thenoverload them with parameters defined here. All parameters
    are optional.
    The plugin should be declared in each struts-config file.
    - definitions-config: (optional)
    Specify configuration file names. There can be several comma
    separated file names (default: ?? )
    - moduleAware: (optional - struts1.1)
    Specify if the Tiles definition factory is module aware. If true
    (default), there will be one factory for each Struts module.
    If false, there will be one common factory for all module. In this
    later case, it is still needed to declare one plugin per module.
    The factory will be initialized with parameters found in the first
    initialized plugin (generally the one associated with the default
    module).
    true : One factory per module. (default)
    false : one single shared factory for all modules
    - definitions-parser-validate: (optional)
    Specify if xml parser should validate the Tiles configuration file.
    true : validate. DTD should be specified in file header (default)
    false : no validation

    Paths found in Tiles definitions are relative to the main context.
    -->
    <plug-in className="org.apache.struts.tiles.TilesPlugin" >
        <set-property property="definitions-config" value="/WEB-INF/tiles-defs.xml" />      
        <set-property property="moduleAware" value="true" />
    </plug-in>
    
    <!-- ========================= Validator plugin ================================= -->
    <plug-in className="org.apache.struts.validator.ValidatorPlugIn">
        <set-property
            property="pathnames"
            value="/WEB-INF/validator-rules.xml,/WEB-INF/validation.xml"/>
    </plug-in>
  
</struts-config>
